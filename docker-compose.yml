version: '2'
services:
  web:
    image: nginx:latest
    hostname: web
    container_name: web
    restart: unless-stopped
    stdin_open: true
    tty: true
    volumes:
        - ./html:/usr/share/nginx/html
        - ./default.conf:/etc/nginx/conf.d/default.conf
    ports:
        - 80:80/tcp
